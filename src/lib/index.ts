// ─── Barrel: $lib entry point ─────────────────────────────
// Re-export config so components can import from '$lib'
// directly if preferred, rather than '$lib/config'.

// Re-export config so components can import from '$lib' directly if preferred
export { site, navLinks } from './config';
